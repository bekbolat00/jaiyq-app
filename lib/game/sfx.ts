/**
 * Звуки «Забей гол». Аудиофайлов пока нет — функции ничего не делают,
 * но вызываются в нужные моменты, чтобы звук подключался в одном месте.
 */

/** Касание ноги и мяча. `pace` 0..1 — насколько сильный удар (громкость, тембр). */
export function playKickSound(pace: number) {
  void pace;
}
