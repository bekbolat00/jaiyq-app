# ФК Жайык — дизайн-система «Matchday»

Характер: официальный клуб из Уральска. Ночной Жайык (Урал) — глубокий
тёмно-синий, как щит герба, и точный голубой контур, как линии на гербе.
Спокойно, дорого, современно. Эмоция — только в важные моменты
(LIVE, гол, победа, билет).

Главное правило: **сдержанность + точность**. Ничего не светится без причины.

---

## Цвет (из герба)

Герб: щит `#0E1F73`, контур и буквы `#00F5F5`, волны `#00C8D8` и `#0A7FA8`.

| Токен (Tailwind) | Значение | Роль |
|---|---|---|
| `bg-background` | `#050A1C` | фон приложения (ночной синий, НЕ чёрный) |
| `bg-surface` | `#0A1433` | карточки, списки |
| `bg-surface-2` | `#0F1C45` | приподнятое: нажатое, вложенный блок, чип |
| `bg-navy` | `#0E1F73` | фирменный синий: hero матча, активные клубные блоки |
| `text-accent` / `bg-accent` | `#00E8F0` | фирменный голубой — действие, выбранное, LIVE-счёт Жайыка |
| `text-wave` | `#00C8D8` | вторичный голубой (иконки, прогресс) |
| `text-deep` | `#0A7FA8` | третичный (декор волн) |
| `text-foreground` | `#F3F6FF` | основной текст |
| `text-muted` | `#A3AFCF` | вторичный текст |
| `text-subtle` | `#6B78A0` | третичный текст (≥13px) |
| `border-line` | `rgba(150,180,255,0.10)` | границы, разделители |
| `border-line-strong` | `rgba(150,180,255,0.18)` | граница при фокусе/выборе |
| `text-win` / `bg-win` | `#34D399` | победа, успех |
| `text-draw` | `#FBBF24` | ничья, предупреждение |
| `text-loss` | `#F87171` | поражение, ошибка |
| `text-live` / `bg-live` | `#FF4D5E` | LIVE |
| `text-on-accent` | `#041226` | текст на голубой кнопке |

Запрещено: новые hex в компонентах, зелёные/золотые/розовые акценты,
градиенты из нескольких голубых, `text-emerald-*`, `text-rose-*`,
`#020408`/`#020308`.

Голубой `accent` на экране — максимум в 2–3 местах: одна главная кнопка,
выбранное состояние, живое значение.

## Типографика

Шрифт Geist (кириллица). Числа — `tabular-nums`. **Обычный регистр.**
КАПС только у `t-label` и бейджей (LIVE, В/Н/П).

Утилиты из `globals.css`:

| Класс | Размер/строка | Вес | Где |
|---|---|---|---|
| `t-display` | 44/48, −0.03em | 700 | счёт, цена, большое число |
| `t-h1` | 28/34, −0.02em | 700 | заголовок экрана |
| `t-h2` | 20/26, −0.01em | 600 | заголовок секции |
| `t-h3` | 17/22 | 600 | название матча, игрока, товара |
| `t-body` | 15/22 | 400 | основной текст |
| `t-small` | 13/18 | 400 | подписи, дата, описание |
| `t-caption` | 12/16 | 500 | мета |
| `t-label` | 11/14, +0.08em, КАПС | 600 | надзаголовок, максимум 1 на блок |

Запрещено: `font-black`, `font-extrabold`, `tracking-[0.2em]` и шире,
текст меньше 11px, `text-[7px]…[10px]`, разрядка у заголовков.

## Радиусы

`rounded-lg` 8 (чип, бейдж, миниатюра) · `rounded-xl` 12 (кнопка, поле,
плитка) · `rounded-2xl` 16 (карточка) · `rounded-3xl` 24 (верх шторки,
hero) · `rounded-full` — только аватар, переключатель, LIVE-точка.

## Отступы

4 · 8 · 12 · 16 · 24 · 32 · 48. Поля экрана 16 (`px-4`). Между секциями
32 (`gap-8`). Заголовок секции → контент 12. Внутри карточки 16 (`p-4`).
Между карточками 12. Строка списка ≥56px.

## Поверхности

- Карточка = `rounded-2xl bg-surface border border-line`. Без теней,
  без blur, без внутренних бликов, без градиентов.
- Карточка внутри карточки — нельзя. Внутри используем разделитель
  `divide-y divide-line` или `bg-surface-2` без рамки.
- Список однотипного (матчи, результаты, настройки) = ОДНА поверхность со
  строками и разделителями, а не стопка карточек.
- Стекло (`backdrop-blur`) — только нижнее меню и липкая шапка.
- Классы `glass`, `glass-strong`, `glass-premium`, `neon-cyan`,
  `neon-cyan-surface`, `accent-glow*` устарели: в `globals.css` они
  приглушены до обычной поверхности, в новом коде не использовать.

## Компоненты (`app/components/ui`)

- `Button` — `variant`: `primary` (голубой, 52px), `secondary`
  (`surface-2`), `ghost` (текст голубой), `danger`; `size`: `lg` 52 / `md`
  44 / `sm` 36; `loading`, `disabled`, `icon`. Нажатие: scale .97, 120мс,
  хаптик. **Одна `primary` на экран.** Текст обычным регистром.
- `SectionHeader` — `title` (t-h2) + необязательное действие справа
  («Все», ghost).
- `Tabs` — подчёркивание 2px голубым, переезжает через `layoutId`,
  хаптик `select`. Никаких «таблеток» с заливкой.
- `ResultBadge` — В / Н / П: 22×22, `rounded-md`, цвет win/draw/loss 16% фон
  + цветной текст.
- `LiveBadge` — красная точка + текст, КАПС 11px.
- `BrandWaves` — три волны из герба (SVG), декор ТОЛЬКО для hero матча,
  шапки профиля и пустых состояний, `opacity` ≤ 0.35.
- `Crest` — логотип команды в круге `bg-surface-2` заданного размера.
- `EmptyState` — иконка 24 + текст + необязательное действие.

## Иконки

Только `lucide-react`, `strokeWidth={1.75}`, 20px в интерфейсе, 24px в
меню. Эмодзи как иконки — нельзя (⚡🎟⚽🟨🟥🔄🔥👕).
Футбол: `lucide` `Goal`/`CircleDot` для гола, прямоугольник 10×14
`rounded-[2px] bg-draw`/`bg-loss` для карточек, `ArrowLeftRight` для замены.

## Движение

- Fast 120мс ease-out — нажатие, переключатель.
- Normal 220мс `[0.2, 0.8, 0.2, 1]` — табы, появление, смена контента.
- Slow — spring `stiffness 380, damping 36` — шторки.
- Появление списка: `y: 8 → 0`, `opacity`, stagger 40мс, максимум 6 элементов.
- Бесконечная анимация только у LIVE-точки.

## Тексты

Спокойный тон официального клуба. «Купить билет», «Сделать прогноз»,
«Все матчи». Без «по-пацански», без латиницы в русских подписях
(не «ZHAIYQ ЭКСПЕРТ», а «Прогноз»; не «JERSEY», а «Форма»).
